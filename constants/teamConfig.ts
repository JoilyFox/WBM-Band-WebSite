/**
 * Team member interaction configuration constants
 */

/** Delay in milliseconds between each card's auto-rotation start (stagger effect) */
export const AUTO_ROTATION_STAGGER_MS = 2000

/** Intersection observer threshold (0.1 = 10% of element must be visible) */
export const VIEWPORT_THRESHOLD = 0.1

/** Debounce delay in milliseconds for hover events */
export const HOVER_DEBOUNCE_MS = 70

/** Image crossfade transition duration in milliseconds */
export const TRANSITION_DURATION_MS = 150

/** Auto-rotation interval in milliseconds (time between image changes) */
export const AUTO_ROTATION_INTERVAL_MS = 10000
