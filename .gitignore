node_modules
*.log*
.nuxt
.nitro
.cache
.output
.data

# Test coverage output
coverage
.env
.env.production
.env.local
.env.*.local
*.env
.npmrc
dist

# macOS Finder metadata
.DS_Store

# Secrets — GA4 / Google Cloud service-account keys must NEVER be committed.
# Keep the JSON key OUTSIDE the repo; these patterns are a safety net.
*service-account*.json
*-sa-key*.json
gcp-*.json
ga4-*.json
**/*.credentials.jsonscreenshots/

# Output of scripts/screenshot-devices.mjs (regenerable device captures)
screenshots/
